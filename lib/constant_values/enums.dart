enum FontFamily { hermann, raleway, tajawal }

enum Users { freelancer, employee }

enum RequestFrom { email, phone }

enum DisplayType { login, register, forget }

enum Tabs { home, booking, message, profile, newService }

enum Chat { seen, delivered, send }

enum Leaves { Pending, Reject, Approved }

enum Environment { staging, local, live }

enum Method { POST, PUT, DELETE, GET, PATCH }

enum BookingDecision { ACCEPT, CANCEL, COMPLETED }

enum Social { Thread, Twitter, Instagram, Facebook, Linkedin }

enum LeaveTypes { SICK, EMERGENCY, VACATION, CASUAL }
